# OpenID Connect Server Environment Variables
# Copy this file to .env and customize as needed

# MongoDB Configuration (used when --profile with-mongodb is active)
MONGO_USER=admin
MONGO_PASSWORD=changeme
MONGO_DATABASE=openid

# Server Configuration (optional overrides)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8080

# Setup Configuration (for non-interactive setup)
# ISSUER_URL=http://localhost:8080
# ADMIN_USER=admin
# ADMIN_PASS=secret123

# Storage Configuration (optional explicit override)
# By default, storage is auto-detected:
# - If MONGODB_URI is set, uses MongoDB
# - Otherwise uses JSON file (data/openid.json)
# STORAGE_TYPE=json
# STORAGE_JSON_FILE=data/openid.json
# STORAGE_MONGO_URI=mongodb://admin:changeme@mongodb:27017/openid?authSource=admin
